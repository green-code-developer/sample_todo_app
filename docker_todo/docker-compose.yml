version: '3'
services:
  postgres:
    build:
      context: ./postgres
    ports:
      - 52532:5432
    environment:
      - POSTGRES_USER=todo
      - POSTGRES_PASSWORD=todo
      - POSTGRES_DB=todo
